apiVersion: apps/v1
kind: Deployment
metadata:
  name: visitor-app
spec:
  template:
    spec:
      containers:
      - name: visitor-app
        env:
        - name: ENVIRONMENT
          value: "production"
        resources:
          requests:
            memory: "256Mi"
            cpu: "200m"
          limits:
            memory: "512Mi"
            cpu: "500m"
